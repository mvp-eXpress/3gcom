syntax = "proto3";

package pim.v1;

option go_package = "pimpb";

// All times and timestamps are seconds from epoch in UTC. In other words, the
// number of seconds from the epoch of 1970-01-01T00:00:00Z to now.

message Empty {};

